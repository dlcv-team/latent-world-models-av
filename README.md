# latent-world-models-av
DriveWAM benchmarks pretrained visual representations (ViT, DINOv2, V-JEPA, CLIP) for latent-space autonomous driving world models. By probing frozen embeddings to predict CAN-bus ego-actions, we evaluate which encoders best preserve the spatial and temporal dynamics required for action-conditioned planning.
